# Frontend API Configuration
# By default, the frontend will call http://localhost:5000 (backend running locally)
# For Docker deployments, set this to the backend service name (e.g., http://backend:5000)

REACT_APP_API_URL=http://localhost:5000
